// Write a Program to find the integer
#include <stdio.h>
int main()
{
    int a, b, sum;
    printf("Enter the number:");
    scanf("%d", &a);
    printf("Enter the number:");
    scanf("%d", &b);
    sum = a + b;
    printf("The required sum is = %d", sum);
    return 0;
}